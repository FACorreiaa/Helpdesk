import {
    makeStyles
} from '@material-ui/core/styles';


export const classes = makeStyles(theme => ({
    button: {
        margin: theme.spacing(1),
    },
    input: {
        display: 'none',
    },
}));